# GraphRAG Agent

Welcome! Ask a question and the agent will retrieve context from the knowledge graph and vector store to generate an answer.

> **Note:** The generation model is currently a placeholder. Responses will be minimal until the model node is wired up.
